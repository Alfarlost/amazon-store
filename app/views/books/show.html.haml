%h3.text-center 
  Full Information
  .row
    .col-xs-12
      = render "book_row", book: @book, orderitem: @orderitem
  .row
    %h4
      Reviews:
      = link_to "(Add new Review)", new_book_rating_path(@book.id)
    - @ratings.each do |rating|
      - if rating.approved?
        = render "review", rating: rating